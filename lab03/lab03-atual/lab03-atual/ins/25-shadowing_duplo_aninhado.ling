fn sombraDupla(a: i32, b: i32) -> i32 {
    let mut v: i32;
  v = a * 10;
    let mut w: i32;
  w = b + 1;
  {
    let mut v: i32;
    v = w * 2;
    {
    let mut w: i32;
      w = v + v;
      return w;
    }
  }
}